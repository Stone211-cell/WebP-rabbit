# วิธีใช้ระบบ Zod Error Handling

## 🎯 เป้าหมาย
แค่เขียน Zod schema กำหนด validation → ระบบจัดการ error ให้อัตโนมัติ!

## 📝 3 ขั้นตอนง่ายๆ

### 1️⃣ สร้าง Zod Schema (แค่กำหนด validation)
```typescript
// lib/validate/Zod.ts
export const profileSchema = z.object({
    name: z.string().min(3, "ชื่อต้องมีอย่างน้อย 3 ตัวอักษร"),
    email: z.string().email("รูปแบบอีเมลไม่ถูกต้อง"),
    phone: z.string().min(10, "เบอร์โทรต้องมีอย่างน้อย 10 หลัก"),
})
```

### 2️⃣ ใช้ใน API Route (แค่ parse)
```typescript
// app/api/profile/route.ts
try {
    const data = profileSchema.parse(body); // 👈 แค่นี้!
    await prisma.profile.create({ data });
    return NextResponse.json({ success: true });
} catch (error) {
    return renderError(error); // 👈 แค่นี้!
}
```

### 3️⃣ ใช้ใน Client (แค่ catch)
```typescript
// app/profilecreate/page.tsx
try {
    const { data } = await axiosInstance.post("/profile", form);
    toast.success("สำเร็จ!");
} catch (error) {
    handleApiError(error); // 👈 แค่นี้!
}
```

## ✅ เสร็จแล้ว!

### ผลลัพธ์:
- ✨ กรอกถูก → แสดง toast สำเร็จ
- ❌ กรอกผิด → แสดง toast error อัตโนมัติ
- 🎯 ไม่ต้องเขียนโค้ดแสดง error เอง

### ไฟล์ที่ต้องยุ่ง:
1. **Zod Schema** - กำหนด validation
2. **API Route** - เขียน business logic
3. **Page (Client)** - เขียน UI และ form

### ไฟล์ที่ไม่ต้องแตะ:
- ❌ `lib/handleError.ts` - ทำงานอัตโนมัติ
- ❌ `lib/rendererror.ts` - ทำงานอัตโนมัติ
